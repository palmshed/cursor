#include "core/session_state.h"

namespace Core {
// SessionState is a simple data holder — all members are inline-initialized.
// No constructor or method definitions needed at this time.
} // namespace Core
